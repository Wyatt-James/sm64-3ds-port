#pragma once

/*
 * Handles the interaction side of the touch-screen menu, and holds some definitions for rendering.
 */

void n3ds_menu_handle_touch();
void n3ds_menu_render(void);
void n3ds_menu_init(void);
